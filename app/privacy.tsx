import React from 'react';
import { StyleSheet, Text} from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const Privacy = () => {
    return (
        <SafeAreaView>
            <Text>Конфіденційність</Text>
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({})

export default Privacy;
